---
phase: 10-dashboard-redesign
plan: 01
type: execute
---

<objective>
Redesign Dashboard layout with Mantine Grid responsive system.

Purpose: Replace custom CSS flex layout with production-ready Mantine Grid system that adapts from desktop (3-column) to mobile (1-column) layouts.
Output: Responsive Dashboard layout using Mantine Container + Grid with proper breakpoints for filters, content, and trends panels.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-redesign/10-RESEARCH.md
@.planning/phases/09-component-library/09-02-SUMMARY.md
@frontend/src/pages/Dashboard.tsx
@frontend/src/pages/Dashboard.css

**Tech stack available:**
- Mantine UI (@mantine/core 8.3.11) - Grid, Container components
- React 19.2.0
- Recharts 3.6.0 (already installed)
- React Query (@tanstack/react-query) - needs installation

**Established patterns:**
- Mantine components wrapped in src/components/ui/ (Phase 9)
- MantineProvider configured in Storybook
- Design tokens from Phase 8 (CSS custom properties)

**Constraining decisions:**
- Phase 9: Mantine UI for all components (don't use custom Tailwind)
- Phase 8: Design tokens in CSS custom properties for theming
- Phase 5: Dashboard split-view: events panel (left), detail panel (right)
- Phase 5: 60-second polling for live updates

**From RESEARCH.md:**
- Use Mantine Grid with span={{ base: 12, md: X }} for responsive breakpoints
- Container fluid for full-width dashboard layouts
- Don't hand-roll responsive grid CSS (use Mantine Grid)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Query and date utilities</name>
  <files>frontend/package.json</files>
  <action>Install @tanstack/react-query (v5) and date-fns (v2) for data fetching/caching and date manipulation. Run: cd frontend && npm install @tanstack/react-query@^5 date-fns@^2. These are required dependencies from the research for dashboard data management and time-series filtering.</action>
  <verify>cd frontend && npm list @tanstack/react-query date-fns shows both installed</verify>
  <done>Both packages installed successfully with correct versions</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Dashboard layout with Mantine Grid</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <action>Replace the custom CSS flex layout (.dashboard-container, .events-panel, .detail-panel) with Mantine Grid system. Import Container and Grid from @mantine/core. Wrap entire dashboard in Container fluid. Use Grid with Grid.Col for 3-panel layout: filters (span={{ base: 12, md: 3 }}), events feed (span={{ base: 12, md: 6 }}), trends/detail (span={{ base: 12, md: 3 }}). Remove Dashboard.css imports and inline className references to dashboard-container, events-panel, detail-panel. Keep existing component logic (filters state, event selection) unchanged - only migrate layout structure. This follows the research pattern from 10-RESEARCH.md Pattern 1 example code.</action>
  <verify>npm run dev starts without errors, visit http://localhost:5173/ and verify Dashboard renders with Mantine Grid (inspect shows mantine-Grid-root classes)</verify>
  <done>Dashboard uses Mantine Container + Grid, no custom layout CSS, responsive breakpoints working (3 cols desktop, 1 col mobile), all components render correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev starts without errors
- [ ] Dashboard page loads at http://localhost:5173/
- [ ] Desktop view (>992px): 3-column layout visible
- [ ] Tablet view (768px): layout adapts
- [ ] Mobile view (375px): single column stacked layout
- [ ] No console errors
- [ ] All existing functionality works (filters, event selection, polling)
</verification>

<success_criteria>

- @tanstack/react-query and date-fns installed
- Dashboard.tsx uses Mantine Container + Grid
- Responsive layout: 3 cols desktop, 1 col mobile
- Custom Dashboard.css layout classes removed
- No TypeScript errors
- All existing features preserved
  </success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-redesign/10-01-SUMMARY.md`:

# Phase 10 Plan 1: Responsive Layout & Grid System Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `frontend/package.json` - Description
- `frontend/src/pages/Dashboard.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 10-02-PLAN.md (Events Feed Redesign)
</output>
